# 二维码（AprilTag）定位导航完整流程

> 覆盖 `tool/qr_odom/` 下全部脚本：生成 → 标定（位姿外参 + 控制死区）→ QR 里程计 → EKF 融合 → 导航。
> 配套设计文档：[`方案_ekf多源里程计融合.md`](./方案_ekf多源里程计融合.md)（EKF 融合的数学推导）。

---

## 1. 整体数据流

```
┌─────────────┐
│ 建图（一次性）│  looper_bridge_node.py(VIO) + build_map_node.py
└──────┬──────┘  → tinynav_db/map_xxx/
       │
       ▼
┌───────────────────────────────────────────────────────────────────┐
│ 运行时：                                                              │
│                                                                       │
│  looper_bridge_node.py ──/slam/odometry──┐                          │
│  (VIO, 高频)                              │                          │
│                                           ▼                          │
│                                    ekf_odom_node.py ──/slam/odometry_fused──┐
│                                           ▲                          │      │
│  odom_node.py ──/qr/odom(间歇)───────────┘                          │      │
│  (看到 AprilTag 板才发)                                              │      │
│                                                                       │      │
│  map_node.py ──world→map TF(稀疏，重定位成功才发)────────────────────┘      │
│                                                                       │      │
│                                                                       ▼      │
│                                            nav_node.py (PI) 或 mpc_nav_node.py (Plan+MPC)
│                                                        │
│                                                        ▼
│                                              /control/cmd_vel → 机器狗
└───────────────────────────────────────────────────────────────────┘
```

目标位姿是**相对于地图**定义的：`T_map_goal = T_map_qrworld @ T_qrworld_robot`，标定一次、写入 `tinynav_db/qrcode/tag_mappose.json` + `tag_target.json`，之后一直有效——机器狗不需要在导航时看到 AprilTag 板（只在标定那一刻需要），每次重新开机、重新定位后都能精确回到同一个位姿。板子固定装在场地里（比如充电桩旁）就是为这个场景设计的。

**关键前提**：`odom_node.py`/`nav_node.py`/`mpc_nav_node.py` 都要用 `world→map` 这条 TF（由 `map_node.py` 重定位成功后广播），所以必须先有一张建好的地图，且运行时 `map_node.py` 必须至少重定位成功过一次。map_node 的定位是**低频、事件驱动**的（DINOv2 embedding + LightGlue 匹配成功才发一次），两次匹配之间什么都不发——这正是引入 EKF 融合、在两次地图重定位之间用 VIO/QR 等源撑住高频位姿估计的原因。

---

## 2. Step 1：生成 AprilTag 板

生成器统一用 `tool/qr_odom/generate.py`：

```bash
python tool/qr_odom/generate.py --grid 2 --size 0.076 --spacing 0.019
```

- 生成 **2×2 GridBoard**（4 个 tag，id 0-3，`AprilTag 36h11` 家族，Hamming 距离 11，误检率最低）。`--size`/`--spacing` 默认值就是当前生产用的 76mm/19mm，日常直接 `python tool/qr_odom/generate.py --grid 2` 即可。
- 输出到 `tinynav_db/qrcode/`：
  - `tag_grid_2x2_s76mm.pdf`（A4，100% 比例打印）
  - `tag_grid_2x2_s76mm.json`（板子参数：`tag_family`/`grid`/`tag_ids`/`size_m`/`spacing_m`，`odom_node.py`/`record_node.py`/`benchmark.py` 都读这个文件）
- **打印后务必用尺子量一下实际 tag 边长**，如果和 `size_m` 有出入，手动改对应 json 里的值（PDF 上有红色标记线标出量取位置）。
- `--grid` 支持任意 N（1/2/3/4...），文件名按 `tag_grid_{N}x{N}_s{size}mm` 区分，互不覆盖，方便同时保留多个尺寸做对比。

---

## 3. Step 2：标定

标定分两类，**互相独立**，都要做：

### 3.1 位姿标定（板子在地图里的位置 + 目标点定义）

```bash
scripts/run_qr_calibrate.sh [map_path]
```

这个脚本用 tmux 拉起 4 个 pane：`looper_bridge_node.py`（VIO）+ `map_node.py`（重定位）+ `rviz` + 最后一个 pane 里敲好了 `python tool/qr_odom/record_node.py` 但**不会自动执行**。操作步骤：

1. 等 `map_node.py` 重定位成功（看 rviz 里 world→map 的 TF 出现，或 pane 1 日志）。
2. 把机器狗摆到你希望的"目标停靠位姿"上，让相机能看到 AprilTag 板。
3. 切到最后一个 pane，按 Enter 触发 `record_node.py`。它会收集 `RECORD_FRAMES=20` 帧有效检测（`solvePnPRansac` 重投影误差 < 3px、可见 tag ≥ 2 个），求平均后写：
   - `tinynav_db/qrcode/tag_target.json`：`T_qrworld_robot`（目标点相对板子的位姿——这就是"机器狗应该停在哪"）
   - `tinynav_db/qrcode/tag_mappose.json`：`T_map_qrworld`（板子在地图里的位姿，**只有 world→map TF 存在时才会写**）

### 3.2 相机 → 机身外参（`robot_frame.py`）

`tool/qr_odom/robot_frame.py` 里存着相机光心相对机身控制中心的固定物理偏移，供 `nav_node.py`/`record_node.py`/`compare_to_target.py`/`mpc_nav_node.py` 统一导入：

```python
CAMERA_FORWARD_FROM_ROBOT_M = 0.30   # 实测：相机在机身控制中心正前方 30cm
CAMERA_LEFT_FROM_ROBOT_M = 0.0
CAMERA_UP_FROM_ROBOT_M = 0.0
```

这个值目前是**手动拿尺子量出来的**，没有做自动标定（原计划的"原地旋转 + 最小二乘解算外参"验证脚本已确认跳过，直接信任实测值）。

**⚠️ 这个值改动后，`3.1` 里生成的 `tag_target.json`/`tag_mappose.json` 语义就变了**（因为标定时也用这个外参把相机位姿换算成机身位姿），**必须重新跑一次 `run_qr_calibrate.sh`**，不能沿用外参改之前标定出来的旧文件。

### 3.3 控制死区标定（`calibrate_cmd_vel_deadzone.py`）

跟"位姿"完全无关，标的是**这台机器狗底盘对小指令的响应特性**：

```bash
python tool/qr_odom/calibrate_cmd_vel_deadzone.py
```

依次发送一串 `angular.z`（`[0.05, 0.08, 0.10, 0.12, 0.15, 0.18, 0.20, 0.25, 0.30, 0.40, 0.50]` rad/s，每个保持 2.5s，用 `/slam/odometry` 的朝向连续累积算实际转速），跑完打印"指令 vs 实测 vs 比值"表，自动标出 `测/发 < 0.5`（大概率在死区里）的几个值。用这个结果去校准 `nav_node.py` / `mpc_nav_node.py` 里的 `MIN_ANGULAR`/`MIN_LINEAR`（发布指令前的死区补偿下限，两个节点都用同一个 `_clip_with_min` 套路：非零但低于这个值的指令会被提到最小有效值，真正的零不受影响）。

**背景**：机器狗在小指令下"发了但不转"是本轮调试实际遇到的问题——之前怀疑过是 MPC 算法 bug，最后定位到更像是底盘死区，所以专门做了这个标定工具。

---

## 4. Step 3：QR 里程计（`odom_node.py`）

```bash
python tool/qr_odom/odom_node.py
```

检测 AprilTag 板 → `solvePnPRansac` 求 `T_camera_board` → 用 `tag_mappose.json` 里的 `T_map_qrworld` 和 TF `world→map` 换算成 `T_world_camera` → 发布 `/qr/odom`（`nav_msgs/Odometry`）。

- 至少 2 个 tag 可见、重投影误差 < 3px 才发布。
- 协方差随重投影误差缩放，写进消息给下游 EKF 做加权。
- **前提**：`tag_mappose.json` 必须已经标定好（3.1 节）。

## 5. Step 4：EKF 融合（`tool/ekf_odom_node.py`）

```bash
python tool/ekf_odom_node.py
```

Error-State EKF，四元数姿态，9 维误差状态（δp/δv/δθ），Joseph form 协方差更新。

- **Predict**：`/slam/odometry`（VIO，高频）驱动。
- **Update**：`/qr/odom`（本文档主角，间歇、绝对位姿，6-DOF）+ `/wheel`、`/lidar`（delta-referenced，未接的话就是没有）+ `/rtk`（3-DOF，位置only）。
- 输出：`/slam/odometry_fused`，`nav_node.py`/`mpc_nav_node.py`/`compare_to_target.py` 都订阅这个。
- 完整数学推导见 [`方案_ekf多源里程计融合.md`](./方案_ekf多源里程计融合.md)。

## 6. Step 5：导航到目标位姿

两个互相独立的实现，目标定义完全一样（`T_map_goal = T_map_qrworld @ T_qrworld_robot`），都订阅 `/slam/odometry_fused`、都发布到 `/control/cmd_vel`。**`nav_node.py` 是当前主用方案，`mpc_nav_node.py` 是备选方案**（还在调试/验证阶段，尚未替代 nav_node.py）：

| | `nav_node.py`（PI，**当前主用**） | `mpc_nav_node.py`（Plan+MPC，**备选，调试中**） |
|---|---|---|
| 运动学假设 | holonomic（可平移+旋转同时进行，两段式：先平移到阈值内再纯转向） | unicycle + 原地自转（不横移，只有 v(前进/后退) + ω(转向)，仿"汽车自动泊车"思路） |
| 控制律 | 两段式 PI（`K_LINEAR`/`K_ANGULAR` + 积分项，`_clip_with_min` 死区补偿） | Plan（turn-drive-turn 梯形加减速曲线）+ MPC（`scipy.optimize` SLSQP 单打靶滚动时域求解，硬加速度约束 + 死区补偿） |
| 控制频率 | 跟着 `/slam/odometry_fused` 回调走 | 独立 20Hz Timer，跟里程计频率解耦 |
| 现状 | 日常默认用这个 | 还在实机调试（Jetson 求解耗时、控制死区等问题还没完全收尾），跑通、稳定后才考虑替换 nav_node.py |

### 6.1 `nav_node.py`（主用）

```bash
python tool/qr_odom/nav_node.py
```

主要常量（在文件顶部）：`MAX_LINEAR=0.3m/s`、`MAX_ANGULAR=0.5rad/s`、`MIN_LINEAR=MIN_ANGULAR=0.15`（死区下限）、`DIST_THRESH=0.06m`、`HEADING_THRESH=0.06rad`。

### 6.2 `mpc_nav_node.py`（备选，调试中）

```bash
python tool/qr_odom/mpc_nav_node.py
```

**Plan 阶段**（闭式、纯函数、不依赖 ROS，方便离线单测）：从当前位姿到固定目标位姿，规划"先原地转向对准目标方位角 → 直行 → 原地转向对准最终朝向"三段轨迹，每段是梯形加减速剖面。edge case（目标很近只需重新定向、起步已对准方位角、终点朝向恰好等于方位角、目标在正后方）都处理了。

**MPC 阶段**：单打靶非线性 MPC，`scipy.optimize.minimize(SLSQP)`，硬约束用 `LinearConstraint` 限制相邻两步的加速度，代价函数是"位置+朝向跟踪 + 控制量 + 控制变化率"的二次型，热启动（shift-and-pad）。每帧发布前对 `v`/`ω` 做死区补偿（同 `_clip_with_min`）。

当前常量（**这几个仍在实机调试中，Jetson 上性能是主要约束**）：

```python
CONTROL_HZ = 20.0
DT_MPC     = 0.05
HORIZON_N  = 6            # 0.3s 前瞻——比较短，是权衡 Jetson 求解速度的结果
HORIZON_N_DEGRADED = 4    # 求解太慢时自动降级
SOLVE_TIME_BUDGET_S = 0.03
MAX_LINEAR      = 0.5     # m/s
MAX_ANGULAR     = 0.8     # rad/s
MAX_LINEAR_ACC  = 0.6     # m/s^2  （沿用 cmd_vel_control.py 的值）
MAX_ANGULAR_ACC = 0.8     # rad/s^2（同上，那边定义了但没用上）
MIN_LINEAR = MIN_ANGULAR = 0.15   # 待用 3.3 节标定结果重新校准
```

**失败兜底**：SLSQP 不收敛 → 复用上一拍指令（有历史的话）或退化成纯 P 控制（没历史，比如第一拍）；求解太慢 → 自动降级 horizon；跟踪误差持续偏大或计划超时 → 重新规划（debounce 2 拍，防止误差抖动触发）。求解失败/降级都有对应的 WARN 日志，失败时还会打印 SLSQP 的 `status`/`nit`/`message`/`fun` 方便排查。

**离线自测**：算法核心是纯函数（不 import 真实 ROS），可以在没有 ROS 环境的机器上 stub 掉 `rclpy`/`geometry_msgs` 等模块直接跑闭环仿真验证（历史上真的用这个方法抓到过一个 `_unicycle_step` 死区导致 SLSQP 卡在零梯度的 bug）。测试脚本是临时写的，不在仓库里，需要时可以照着 `mpc_nav_node.py` 里 `Plan`/`solve_mpc`/`make_plan` 这几个纯函数重新写一份。

### 6.3 诊断工具：`compare_to_target.py`

```bash
python tool/qr_odom/compare_to_target.py
```

同时订阅 `/qr/odom`（原始 QR 观测）和 `/slam/odometry_fused`（EKF 融合后），实时打印两者到标定目标点的**距离误差**和**朝向误差**（yaw，度）。用来对比 `nav_node.py` 和 `mpc_nav_node.py` 的实际对接精度和转弯抖动情况，不需要改代码，两边都能直接用。

---

## 7. 启动脚本速查

| 脚本 | 拉起什么 | 前提 |
|---|---|---|
| `scripts/run_qr_calibrate.sh [map_path]` | looper_bridge + map_node + rviz + `record_node.py`（手动 Enter 触发） | 已建好地图 |
| `scripts/run_qr_ekf.sh [map_path]` | looper_bridge + map_node + `odom_node.py` + `ekf_odom_node.py` + rviz（**不含导航节点**） | `tag_mappose.json` 已存在 |
| `scripts/run_qr_mpc_nav.sh [map_path]` | 同上 + `mpc_nav_node.py` | `tag_mappose.json` + `tag_target.json` 都已存在 |

`run_qr_ekf.sh` 和 `run_qr_mpc_nav.sh` 是分开的两个脚本，方便单独跑融合链路做诊断，或者接上某个导航节点做端到端测试；主用的 `nav_node.py` 目前没有对应的一键脚本，需要单独手动跑（在 `run_qr_ekf.sh` 拉起的融合链路基础上再手动起一个 `python tool/qr_odom/nav_node.py`）。

---

## 8. 文件索引

| 文件 | 角色 |
|---|---|
| `tool/qr_odom/generate.py` | 生成 N×N AprilTag 板 + `tag_grid_NxN_sXXmm.json`（生产用默认 `--grid 2`） |
| `tool/qr_odom/record_node.py` | 标定（`run_qr_calibrate.sh` 用这个），写 `tag_target.json`+`tag_mappose.json` |
| `tool/qr_odom/robot_frame.py` | 相机→机身固定外参（实测 0.30m 前置），全部节点共享 |
| `tool/qr_odom/calibrate_cmd_vel_deadzone.py` | 控制死区标定（跟位姿无关） |
| `tool/qr_odom/odom_node.py` | 检测板子，发 `/qr/odom` |
| `tool/ekf_odom_node.py` | 多源融合，发 `/slam/odometry_fused` |
| `tool/qr_odom/nav_node.py` | 导航（PI，holonomic，**当前主用**） |
| `tool/qr_odom/mpc_nav_node.py` | 导航（Plan+MPC，unicycle，**备选，调试中**） |
| `tool/qr_odom/compare_to_target.py` | 诊断：QR/EKF 到目标点的误差对比 |
| `tinynav_db/qrcode/tag_grid_2x2_s76mm.json` | 板子物理参数（family/ids/size/spacing） |
| `tinynav_db/qrcode/tag_target.json` | `T_qrworld_robot`（目标点相对板子） |
| `tinynav_db/qrcode/tag_mappose.json` | `T_map_qrworld`（板子相对地图，**外参改了要重新生成**） |

话题一览：

| 话题 | 类型 | 发布者 | 订阅者 |
|---|---|---|---|
| `/slam/odometry` | `nav_msgs/Odometry` | `looper_bridge_node.py`（VIO） | `ekf_odom_node.py`、`record_node.py`、`calibrate_cmd_vel_deadzone.py` |
| `/qr/odom` | `nav_msgs/Odometry` | `odom_node.py` | `ekf_odom_node.py`、`compare_to_target.py` |
| `/slam/odometry_fused` | `nav_msgs/Odometry` | `ekf_odom_node.py` | `nav_node.py`、`mpc_nav_node.py`、`compare_to_target.py` |
| `/control/cmd_vel` | `geometry_msgs/Twist` | `nav_node.py` / `mpc_nav_node.py` / `calibrate_cmd_vel_deadzone.py` | 机器狗底盘驱动 |
| `world→map` TF | — | `map_node.py`（重定位成功时，低频） | `odom_node.py`、`nav_node.py`、`mpc_nav_node.py`、`record_node.py`、`compare_to_target.py` |
| `/qr_world/nav_done` | `std_msgs/Bool` | `nav_node.py` / `mpc_nav_node.py` | — |

---

## 9. 已知问题 / 待办

- `tag_mappose.json`/`tag_target.json` 是用**外参修正前**的旧数据标定的，外参改完后**还没重新标定**——用 `mpc_nav_node.py`/`nav_node.py` 实测前必须先重新跑 `run_qr_calibrate.sh`。
- `mpc_nav_node.py` 的 `CONTROL_HZ=20/HORIZON_N=6` 是刚改的，Jetson 上实际求解耗时（`solve_ms`）还没有拿到真实数据验证是否有余量——需要看运行日志里 "MPC solve did not converge" 那行的 `solve_ms=`，或者临时把成功路径也打一下耗时。
- `MIN_LINEAR`/`MIN_ANGULAR`（死区补偿下限，目前 0.15/0.15）还是从 `nav_node.py` 抄来的旧值，没用 `calibrate_cmd_vel_deadzone.py` 实测结果校准过。
