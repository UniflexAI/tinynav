{
  "name": "tinynav-dev",
  "image": "uniflexai/tinynav:3a293f0",
  "context": "..",

  "initializeCommand": "if [ $(uname -m) = 'aarch64' ]; then echo '[\"--privileged\",\"--network\",\"host\"]' > /tmp/runargs.json; else echo '[\"--gpus\",\"all\",\"--privileged\",\"--network\",\"host\"]' > /tmp/runargs.json; fi",

  "runArgs": ["@/tmp/runargs.json"],

  "containerEnv": {
    "DISPLAY": "${localEnv:DISPLAY}",
    "GDK_SCALE": "2"
  },

  "workspaceFolder": "/tinynav",
  "remoteUser": "root",
  "features": {},
  "mounts": [],
  "postCreateCommand": "echo 'Devcontainer ready.'"
}
